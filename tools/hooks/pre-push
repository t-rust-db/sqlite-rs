#!/bin/sh
# Runs the same clippy+fmt gate CI enforces, before it costs a red CI run.
# This is the expensive one (full clippy compile) — kept out of pre-commit
# on purpose. Bypass for a single push with `git push --no-verify`.
set -e

echo "pre-push: make lint (clippy + fmt) — this can take a while"
make lint
