[package]
name = "tree_walker_spike"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlite-rs = { path = "../../.." }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
