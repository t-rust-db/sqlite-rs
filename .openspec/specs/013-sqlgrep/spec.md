# 013 — sqlgrep (moved)

Superseded 2026-09-09. The serverless trigram grep specified here moved to
[t-rust-db/trigrep](https://github.com/t-rust-db/trigrep) as spec 001 (binary
`tg`). This number is retained so ADR-0043 and the 0.22.0 changelog entries
keep resolving; it is not reused.
